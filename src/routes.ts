interface Routes {
  WELCOME: string;
  REGISTER: string;
  LOGIN: string;
  CONTACTS: string;
  USER: string;
}

const routes: Routes = {
  WELCOME: 'welcome',
  REGISTER: 'register',
  LOGIN: 'login',
  CONTACTS: 'contacts',
  USER: 'user',
};

export default routes;
